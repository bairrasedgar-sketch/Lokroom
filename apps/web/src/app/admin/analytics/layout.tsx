export const metadata = {
  title: "Analytics | Admin Lok'Room",
  description: "Statistiques et graphiques de performance de la plateforme Lok'Room",
};

export default function AnalyticsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
