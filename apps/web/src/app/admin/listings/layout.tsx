export const metadata = {
  title: "Annonces | Admin Lok'Room",
  description: "Moderation et gestion des annonces de la plateforme Lok'Room",
};

export default function ListingsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
