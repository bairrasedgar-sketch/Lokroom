export const metadata = {
  title: "Configuration | Admin Lok'Room",
  description: "Parametres globaux de la plateforme Lok'Room",
};

export default function SettingsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
