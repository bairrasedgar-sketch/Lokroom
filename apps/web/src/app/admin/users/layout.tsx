export const metadata = {
  title: "Utilisateurs | Admin Lok'Room",
  description: "Gestion des utilisateurs de la plateforme Lok'Room",
};

export default function UsersLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
