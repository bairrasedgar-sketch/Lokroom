export const metadata = {
  title: "Litiges | Admin Lok'Room",
  description: "Gestion et resolution des litiges de la plateforme Lok'Room",
};

export default function DisputesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
