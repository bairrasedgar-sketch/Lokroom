export const metadata = {
  title: "Logs d'audit | Admin Lok'Room",
  description: "Historique des actions administratives sur la plateforme Lok'Room",
};

export default function LogsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
