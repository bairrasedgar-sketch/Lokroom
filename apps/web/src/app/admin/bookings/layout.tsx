export const metadata = {
  title: "Reservations | Admin Lok'Room",
  description: "Gestion des reservations de la plateforme Lok'Room",
};

export default function BookingsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
