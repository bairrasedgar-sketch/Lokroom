export const metadata = {
  title: "Messagerie | Admin Lok'Room",
  description: "Envoi de notifications et messages aux utilisateurs de Lok'Room",
};

export default function MessagesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
